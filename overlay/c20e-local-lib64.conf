# Rockchip MPP installs into /usr/local/lib64, which glibc does not search by
# default; without this the VA-API driver fails to load with
# "librockchip_mpp.so.1: cannot open shared object file" and every application
# quietly falls back to software video decode.
/usr/local/lib64
