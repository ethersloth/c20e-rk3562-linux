# Let the logged-in desktop user open the loopback camera, the same way the real
# capture devices are handled (uaccess grants an ACL to the active session).
KERNEL=="video[0-9]*", ATTRS{name}=="C20e Camera", GROUP="video", MODE="0660", TAG+="uaccess"
