# Point libva at the Rockchip MPP VA-API driver.
#
# libva otherwise derives the driver name from the DRM device (here "panfrost",
# which has no VA-API driver) and gives up, so applications fall back to
# software decode. The decoder is a separate block from the GPU on this SoC.
LIBVA_DRIVER_NAME=rockchip
